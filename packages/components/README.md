# Semantic visual components

Component implementations are emitted by the presentation engine from structured SlideSpec, DiagramIR and ChartIR data. Source text is escaped and no arbitrary external JavaScript is accepted.
