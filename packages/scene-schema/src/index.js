export const sceneNodeTypes=['text','shape','image','svg','group','chart','diagram','table','video','raster'];export function validateScene(scene){if(!scene||!Number.isFinite(scene.width)||!Number.isFinite(scene.height)||!Array.isArray(scene.nodes))throw new Error('Invalid Scene IR');for(const node of scene.nodes){if(!sceneNodeTypes.includes(node.type)||!node.bbox)throw new Error(`Invalid scene node ${node.id??'unknown'}`)}return scene}
